import 'package:flutter/material.dart';

class TeacherPayroll extends StatelessWidget {
  const TeacherPayroll({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Teacher Payroll Screen Coming Soon...'),
    );
  }
}
