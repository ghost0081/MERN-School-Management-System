import { Typography } from '@mui/material';

const ParentHomePage = () => {
    return <Typography variant="h5">Welcome, view your child's attendance and assignments from the sidebar.</Typography>;
};

export default ParentHomePage;






